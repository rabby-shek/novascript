import { TokenType, Token } from "./tokenTypes";


export function tokenize(srcCode : string): Token[] {
    const tokens = new Array<Token>();
    const src = srcCode.split("");

    return tokens;
}